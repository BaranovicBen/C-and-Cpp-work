
    // long long from, quantity;
    // while (scanf("%lld %lld", &from, &quantity) == 2) {
    //     if (from < 0 || quantity <= 0) {
    //         printf(ERROR_INVALID_INPUT);
    //         return 1;
    //     }

    //     Problem problem = {from, quantity};
    //     problems[problemCount++] = problem;
    //     printCost();
    // }